function login() {
    var username = document.getElementById('username').value;
    var password = document.getElementById('password').value;

    // You can addlogic to check the credentials here
    console.log('Usernaame: ' + username + ', Password: ' + password );
}